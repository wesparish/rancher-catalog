apiVersion: v1
kind: Service
metadata:
  name: {{ include "verthashminer-amd-daemonset.fullname" . }}
  labels:
    {{- include "verthashminer-amd-daemonset.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "verthashminer-amd-daemonset.selectorLabels" . | nindent 4 }}
