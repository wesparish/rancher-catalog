apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "verthashminer-amd-daemonset.fullname" . }}-test-connection"
  labels:
    {{- include "verthashminer-amd-daemonset.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "verthashminer-amd-daemonset.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
