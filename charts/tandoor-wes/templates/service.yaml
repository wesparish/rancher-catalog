apiVersion: v1
kind: Service
metadata:
  name: {{ include "tandoor-wes.fullname" . }}
  labels:
    {{- include "tandoor-wes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tandoor-wes.selectorLabels" . | nindent 4 }}
