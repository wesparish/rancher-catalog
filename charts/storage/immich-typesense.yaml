# Typesense was dropped from immich-wes's values.yaml in favor of Postgres pgvector (immich
# upstream deprecated it before v1.91.4, the version currently deployed). No live typesense
# pod/PVC exists anymore -- confirmed 2026-07-16. Left commented rather than deleted in case the
# data at rootPath below is ever needed for recovery; the underlying Ceph path was never removed.
#
# ---
# apiVersion: v1
# kind: PersistentVolume
# metadata:
#   name: immich-typesense-data
# spec:
#   accessModes:
#   - ReadWriteMany
#   capacity:
#     storage: 50Gi
#   csi:
#     driver: cephfs.csi.ceph.com
#     nodeStageSecretRef:
#       # node stage secret name
#       name: csi-cephfs-secret
#       # node stage secret namespace where above secret is created
#       namespace: ceph-csi
#     volumeAttributes:
#       # optional file system to be mounted
#       "fsName": "test1"
#       # Required options from storageclass parameters need to be added in volumeAttributes
#       "clusterID": "6c2190ab-5525-4345-8dba-8c3151c4e530"
#       "staticVolume": "true"
#       "rootPath": /k8s-volumes/immich-data/typesense-data/
#     # volumeHandle can be anything, need not to be same
#     # as PV name or volume name. keeping same for brevity
#     volumeHandle: immich-typesense-data
#   persistentVolumeReclaimPolicy: Retain
#   volumeMode: Filesystem
# ---
# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: immich-typesense-data
#   namespace: immich
# spec:
#   accessModes:
#   - ReadWriteMany
#   resources:
#     requests:
#       storage: 50Gi
#   storageClassName: ""
#   volumeMode: Filesystem
#   volumeName: immich-typesense-data
