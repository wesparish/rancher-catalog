apiVersion: v1
kind: Service
metadata:
  name: {{ template "octopi.fullname" . }}
  labels:
    app: {{ template "octopi.name" . }}
    chart: {{ template "octopi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
