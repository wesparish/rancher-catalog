apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "squid-wes.fullname" . }}-test-connection"
  labels:
    {{- include "squid-wes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "squid-wes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
