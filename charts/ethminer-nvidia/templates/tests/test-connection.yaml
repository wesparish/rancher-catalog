apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ethminer-nvidia.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "ethminer-nvidia.name" . }}
    helm.sh/chart: {{ include "ethminer-nvidia.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ethminer-nvidia.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
