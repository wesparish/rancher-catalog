apiVersion: v1
kind: Service
metadata:
  name: {{ include "immich-wes.fullname" . }}
  labels:
    {{- include "immich-wes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "immich-wes.selectorLabels" . | nindent 4 }}
