apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "immich-wes.fullname" . }}-test-connection"
  labels:
    {{- include "immich-wes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "immich-wes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
