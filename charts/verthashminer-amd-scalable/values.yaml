# Default values for verthashminer-amd-scalable.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: nexus-jamie-docker.elastiscale.net/verthashminer
  tag: amdgpu-pro-18.50-0.7.0
  pullPolicy: Always

verthashminer:
  wallet: "3NRZzUYhoFyA9aL7edLA9D4SwcNfAwhhNf"
  pool: "stratum+tcp://mining.hashalot.net:3950"
  options: ""
  scalablelabel: "verthashminer"
  gpusperpod: 6

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  path: /
  hosts:
    - chart-example.local
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

  # Not used in deployment.yaml
  #resources:
  #limits:
  #  amd.com/gpu: 1
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
  #  memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
