apiVersion: v1
kind: Service
metadata:
  name: {{ include "tasks-md-wes.fullname" . }}
  labels:
    {{- include "tasks-md-wes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tasks-md-wes.selectorLabels" . | nindent 4 }}
