{{- range untilStep (int .Values.ethminer.minGPUs) (int (add .Values.ethminer.maxGPUs 1)) 1 }}
---
# DaemonSet instance #{{ . }}
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: "{{ include "ethminer-nvidia-daemonset.fullname" $ }}-{{ . }}"
  labels:
    {{- include "ethminer-nvidia-daemonset.labels" $ | nindent 4 }}
    ethminer-instance-num: "{{ . }}"
spec:
  selector:
    matchLabels:
      {{- include "ethminer-nvidia-daemonset.selectorLabels" $ | nindent 6 }}
      ethminer-instance-num: "{{ . }}"
  template:
    metadata:
    {{- with $.Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      labels:
        {{- include "ethminer-nvidia-daemonset.selectorLabels" $ | nindent 8 }}
        ethminer-instance-num: "{{ . }}"
    spec:
      {{- with $.Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml $.Values.podSecurityContext | nindent 8 }}
      containers:
        - name: "{{ $.Chart.Name }}-{{.}}"
          env:
            - name: LOCATION
              value: "{{$.Values.ethminer.location }}"
            - name: WALLET_ADDRESS
              value: "{{$.Values.ethminer.wallet }}"
            - name: OPTS
              value: "{{$.Values.ethminer.options }}"
            - name: HOSTS
              value: "{{$.Values.ethminer.poolhosts }}"
            - name: PASSPHRASE
              value: "{{$.Values.ethminer.passphrase }}"
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          securityContext:
            {{- toYaml $.Values.securityContext | nindent 12 }}
          image: "{{ $.Values.image.repository }}:{{ $.Values.image.tag | default $.Chart.AppVersion }}"
          imagePullPolicy: {{ $.Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            exec:
              command:
                - "pgrep"
                - "ethminer"
          readinessProbe:
            exec:
              command:
                - "pgrep"
                - "ethminer"
          startupProbe:
            failureThreshold: 30
            periodSeconds: 10
            exec:
              command:
                - "pgrep"
                - "ethminer"
          resources:
            limits:
              amd.com/gpu: {{ . }}
            {{- with $.Values.resources }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            - mountPath: /sys
              name: sys
      volumes:
        - hostPath:
            path: /sys
            type: ""
          name: sys
      nodeSelector:
        miner: {{ $.Values.ethminer.scalablelabel }}-{{ . }}
      {{- with $.Values.nodeSelector }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $.Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $.Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
