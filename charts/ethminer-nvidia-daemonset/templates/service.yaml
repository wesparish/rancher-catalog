apiVersion: v1
kind: Service
metadata:
  name: {{ include "ethminer-nvidia-daemonset.fullname" . }}
  labels:
    {{- include "ethminer-nvidia-daemonset.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ethminer-nvidia-daemonset.selectorLabels" . | nindent 4 }}
