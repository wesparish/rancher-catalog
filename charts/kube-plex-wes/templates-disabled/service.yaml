apiVersion: v1
kind: Service
metadata:
  name: {{ template "kube-plex-wes.fullname" . }}
  labels:
    app: {{ template "kube-plex-wes.name" . }}
    chart: {{ template "kube-plex-wes.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "kube-plex-wes.name" . }}
    release: {{ .Release.Name }}
