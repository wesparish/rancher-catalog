apiVersion: v1
kind: Service
metadata:
  name: {{ include "autoscale-test.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "autoscale-test.name" . }}
    helm.sh/chart: {{ include "autoscale-test.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "autoscale-test.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
