apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "autoscale-test.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "autoscale-test.name" . }}
    helm.sh/chart: {{ include "autoscale-test.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "autoscale-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
