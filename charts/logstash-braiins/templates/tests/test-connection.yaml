apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "logstash-braiins.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "logstash-braiins.name" . }}
    helm.sh/chart: {{ include "logstash-braiins.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "logstash-braiins.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
