{{- if .Values.maintainerr.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "plex-wes.fullname" . }}-maintainerr
  {{- with .Values.maintainerr.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  rules:
    - host: {{ .Values.maintainerr.ingress.fqdn }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "plex-wes.fullname" . }}-maintainerr
                port:
                  number: {{ .Values.maintainerr.service.port }}
  tls:
    - hosts:
      - {{ .Values.maintainerr.ingress.fqdn }}
      secretName: {{ .Values.maintainerr.ingress.secretName }}
{{- end }}
