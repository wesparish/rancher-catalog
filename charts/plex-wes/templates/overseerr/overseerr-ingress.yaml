{{- if .Values.seerr.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "plex-wes.fullname" . }}-seerr
  {{- with .Values.seerr.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  rules:
    - host: {{ .Values.seerr.ingress.fqdn }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "plex-wes.fullname" . }}-seerr
                port:
                  number: {{ .Values.seerr.service.port }}
  tls:
    - hosts:
      - {{ .Values.seerr.ingress.fqdn }}
      secretName: {{ .Values.seerr.ingress.secretName }}
{{- end }}
